# Edge AI Architecture — Why it matters

A model that works in a notebook is not a system. Production edge AI has
to answer: how does data get collected from the field, how do we know a
deployed model is still accurate months later, how do we roll out a fix
to a subset of devices, and what happens when the network is down. These
are systems-engineering problems layered on top of the ML problem.
