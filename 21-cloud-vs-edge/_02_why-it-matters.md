# Cloud vs. Edge vs. Hybrid — Why it matters

Getting this choice wrong is expensive to undo: it affects model size
budgets, latency guarantees, infrastructure cost structure (opex vs.
device bill-of-materials), privacy posture, and how quickly you can ship
model improvements. Many teams default to "just call a cloud API" and
only discover the cost/latency/offline problems at scale.
